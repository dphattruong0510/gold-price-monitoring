name: 'gold_dbt'
version: '1.0.0'
config-version: 2

profile: 'gold_dbt'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  gold_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table